<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>フォームデザイン</title>
    <%= stylesheet_link_tag 'styles' %>
</head>
<body>
    <div class="container">
        <div class="progress-bar">
            <div class="step active">
                <span class="step-number">1</span>
                お届け先情報
            </div>
            <div class="step">
                <span class="step-number">2</span>
                ご依頼主情報
            </div>
            <div class="step">
                <span class="step-number">3</span>
                商品情報
            </div>
        </div>
        <%= form_with url: '#', method: :post do %>
            <div class="form-group">
                <label for="phone">電話番号(半角)</label>
                <div class="input-group">
                    <%= text_field_tag 'phone1', nil, placeholder: '000', maxlength: 3 %>
                    <span>-</span>
                    <%= text_field_tag 'phone2', nil, placeholder: '0000', maxlength: 4 %>
                    <span>-</span>
                    <%= text_field_tag 'phone3', nil, placeholder: '0000', maxlength: 4 %>
                    <span class="required">必須</span>
                </div>
            </div>

            <div class="form-group">
                <label for="postal-code">郵便番号(半角)</label>
                <div class="input-group">
                    <%= text_field_tag 'postal_code1', nil, placeholder: '000', maxlength: 3 %>
                    <span>-</span>
                    <%= text_field_tag 'postal_code2', nil, placeholder: '0000', maxlength: 4 %>
                    <span class="required">必須</span>
                </div>
            </div>

            <div class="form-group">
                <label for="address1">ご住所1(都道府県)</label>
                <%= text_field_tag 'address1', nil, placeholder: '都道府県' %>
                <span class="required">必須</span>
            </div>

            <div class="form-group">
                <label for="address2">ご住所2(市区群町村)</label>
                <%= text_field_tag 'address2', nil, placeholder: '市区群町村' %>
                <span class="required">必須</span>
            </div>

            <div class="form-group">
                <label for="address3">ご住所3(番地)</label>
                <%= text_field_tag 'address3', nil, placeholder: '番地' %>
                <span class="required">必須</span>
            </div>

            <div class="form-group">
                <label for="address4">ご住所4(建物名・部屋番号)</label>
                <%= text_field_tag 'address4', nil, placeholder: '建物名・部屋番号' %>
            </div>

            <div class="form-group">
                <label for="company">会社名</label>
                <%= text_field_tag 'company', nil, placeholder: '会社名' %>
            </div>

            <div class="form-group">
                <label for="department">部署名</label>
                <%= text_field_tag 'department', nil, placeholder: '部署名' %>
            </div>

            <div class="form-group">
                <label for="name">お名前</label>
                <div class="input-group">
                    <%= text_field_tag 'lastname', nil, placeholder: '姓' %>
                    <%= text_field_tag 'firstname', nil, placeholder: '名' %>
                    <span class="required">必須</span>
                </div>
            </div>

            <%= submit_tag '次へ', class: 'submit-button' %>
        <% end %>
    </div>
</body>
</html>
